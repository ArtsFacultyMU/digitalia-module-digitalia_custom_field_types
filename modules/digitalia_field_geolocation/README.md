# Digitalia Geolocation Field

A custom Drupal Field integrated with GeoNames.

Optional specification of location type from schemas CCMM and VRA.

Roadmap:

- prefill GeoNames URL based on free text over api (keep option to enter URL and free text place name directly)
- make lat and lang fields usable in Geolocation field ecosystem (preview on map, search on map)
